// Dirección del backend de Editoriales (puerto 3003 en local).
// Tras desplegar el backend reemplace por la URL real, por ejemplo:
// "https://backend-editoriales.onrender.com"
const API_URL = "http://localhost:3003";

// URL pública del mini-sitio de Reseñas. En local queda relativa;
// tras publicar reemplace por la URL real del sitio desplegado.
const RESENAS_URL = "../site-resenas/index.html";
