// Dirección del backend de Libros.
// En local (npm start dentro de backend-libros) queda en el puerto 3001.
// Tras desplegar el backend (Render, Railway, Cyclic, etc.) reemplace este valor
// por la URL real, por ejemplo: "https://backend-libros.onrender.com"
const API_URL = "http://localhost:3001";

// URL pública del mini-sitio de Reseñas (ejercicio 2 de la Tarea 3).
// En local apunta al archivo relativo; tras publicar reemplace por la URL real,
// por ejemplo: "https://mi-sitio-resenas.netlify.app/index.html"
const RESENAS_URL = "../site-resenas/index.html";
